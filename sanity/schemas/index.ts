import { projectSchema } from './project';
import { technologySchema } from './technology';
import { siteContentSchema } from './siteContent';

export const schemaTypes = [projectSchema, technologySchema, siteContentSchema];
